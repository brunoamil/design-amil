import React from 'react';
import Button from './Button';

export default {
    title: "Button"
}

export const normal = () => <Button theme="normal" />;

export const disable  = () => <Button theme="disable" />;